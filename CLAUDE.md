# Live-Data — Upstox WebSocket V3 Market Feed + F&O Analysis

Streams/fetches live Indian market data from Upstox and analyses it (technicals +
option chain + latest news + historical precedent) to surface risk-managed F&O trade
ideas. Risk-first decision-support — NOT guaranteed profit.

## Project structure
```
Live-Data/
├── README.md            # human daily-use guide + full strategy
├── CLAUDE.md            # this file (auto-loaded each session)
├── pom.xml              # Maven build (Java 21) -> target/upstox-fno-pipeline.jar
├── mvnw / mvnw.cmd      # Maven Wrapper (no system Maven needed)
├── .env                 # UPSTOX_API_KEY / _API_SECRET / _REDIRECT_URI (gitignored)
├── .access_token        # generated daily by auth/get-token (gitignored)
├── trade_journal.csv    # generated by journal (gitignored)
├── src/main/java/com/zaheenmunshi/upstox/   # the Java tools
├── docs/                # TRADING_RULES.md, STRATEGIES.md, PIPELINE.md, WORKFLOW.md
├── config/              # positions.example.json (copy -> positions.json)
├── data/                # market data output (gitignored)
├── scratch/             # throwaway one-off analysis scripts (gitignored)
└── .claude/agents/      # the agents
```
**Java is the sole implementation** (Oracle JDK 21, built on the official
`com.upstox.api:upstox-java-sdk`). Build once with `.\mvnw.cmd -q clean package`
(or `./mvnw` on *nix), then run every tool as a subcommand of the uber-jar:
`java -jar target/upstox-fno-pipeline.jar <command> [args]`. All tools anchor file
paths to the project root, so CWD doesn't matter.

**Throwaway scripts:** if an agent or analysis step needs a one-off/ad-hoc script (not a permanent tool), write it to `scratch/` — NEVER to the project root, `src/`, or `src/main/`. `scratch/` is gitignored and safe to empty anytime.

## Commands (`java -jar target/upstox-fno-pipeline.jar <command>`)
Each maps to a class in `src/main/java/com/zaheenmunshi/upstox/`:
- `pipeline ["<redirect-url>"]` (`RunPipeline`) — one-command DATA stage: token (optional) → snapshot → readiness report. Entry point for the orchestrated pipeline (see `docs/PIPELINE.md`).
- `auth` (`Auth`) — interactive daily Upstox OAuth2 login; saves `.access_token`.
- `get-token "<code-or-redirect-url>"` (`GetToken`) — non-interactive token exchange (paste-the-URL flow).
- `snapshot` (`MarketSnapshot`) — fresh REST snapshot → `data/snapshot_*.json`: market status, intraday (5m) + daily candles, nearest-expiry option chain (OI/IV/greeks).
- `stream` (`Streamer`) — WebSocket V3 tick stream → `data/market_data_*.json` every 5s.
- `backtest [--flags]` (`Backtest`) — validate a directional setup on historical candles (win-rate/expectancy/PF/drawdown). Underlying-signal only, not option P&L.
- `monitor` (`MonitorPositions`) — watch open trades in `config/positions.json` vs stop/target/time-stop via live LTP (alert-only).
- `journal add|stats` (`Journal`) — log trades → `trade_journal.csv`; compute real win-rate/expectancy/cost-drag.
- `cleanup [--flags]` (`CleanupData`) — safely delete generated data in `data/` (+ optional `__pycache__`). Flags: `--keep-latest N`, `--older-than N`, `--pycache`, `--dry-run`. Path-guarded to `data/` only.

## Docs (`docs/`)
- `docs/TRADING_RULES.md` — master rulebook: hard risk rules, 9-point pre-trade checklist, regime/IV playbook, strategy matrix, "do not trade" conditions. Trade agents must obey it.
- `docs/STRATEGIES.md` — options strategy playbook (long/debit/credit spreads, straddle/strangle, iron condor/fly, calendar, expiry/0DTE).
- `docs/PIPELINE.md` — orchestrated pipeline stage graph + rules. `docs/WORKFLOW.md` — full system diagram (Mermaid + ASCII).

## Agents (`.claude/agents/`)
- `news-scanner` — latest same-day news/cues → directional bias + key levels.
- `fno-strategist` — builds the F&O trade card per `docs/TRADING_RULES.md` + `docs/STRATEGIES.md`; scores the 9-point checklist.
- `risk-manager` — independent gatekeeper; re-checks a proposed trade vs the rulebook → APPROVE / APPROVE-WITH-CHANGES / VETO.
- `backtester` — validates a setup's historical edge via the `backtest` command before it's trusted.
- `position-monitor` — watches open trades vs stop/target/time-stop (uses the `monitor` command); alert-only.
- `trade-journal` — logs trades and reports real win-rate/expectancy/cost-drag (uses the `journal` command).
- `market-analyst` — general technical analysis engine for any instrument/category.
- `data-janitor` — safely cleans up generated data on request (uses the `cleanup` command); only `data/` + `__pycache__`, never source/creds/token.

## Daily pipeline (ONE-SHOT, AUTONOMOUS — suggestion-only; freshness enforced)
Full stage graph + rules: `docs/PIPELINE.md`. **SUGGESTION-ONLY: never place/modify/cancel orders** (TRADING_RULES §0) — output is always a suggestion the user executes manually.

**Flow:** user pastes the login URL → **ask intent ONCE** (category e.g. F&O intraday / positional, underlying, capital, any bullish/bearish lean) → then run the ENTIRE pipeline autonomously and silently, with **NO intermediate approvals or stage-by-stage check-ins**:
  `pipeline` command (token → snapshot) → `news-scanner` ∥ `backtester` → `fno-strategist` → `risk-manager` (**INTERNAL** check) → present **ONLY the final suggestion** (a trade card, or "stay flat").
- Fold the risk-manager verdict into the single final card; do NOT surface it as a separate APPROVE/VETO step.
- **Interrupt the user ONLY if something errors/breaks** (token/data/API failure) or intent is genuinely ambiguous.
- After the user says they entered: `position-monitor` + `trade-journal` (also suggestion/alert-only).
The main session orchestrates (sub-agents can't spawn sub-agents); run independent stages concurrently.

## Important constraints (do not violate)
- **Suggestion-only — never places/modifies/cancels orders.** No execution code path; the user places every order manually. One-shot flow: ask intent once, then deliver only the final suggestion (no mid-pipeline approvals); interrupt only on error.
- **No guaranteed win rates or ROI.** ~9/10 retail F&O traders lose (SEBI). Risk ≤2% of capital/trade; size from the stop-loss; always include SL + sizing + max loss + own-risk reminder. "No clean setup → stay flat" is valid.
- Requires fresh live data + latest news + historical context; flag low confidence if any are stale/missing.
- Outside market hours: only last-close/snapshot data → lower-confidence analysis.
- Claude can't do the browser login (OAuth + 2FA) — the user does that one step and pastes the redirect URL.

## Current status (2026-06-22)
- **JAVA-ONLY.** All 9 tools are in Java 21 (Oracle JDK) on the official `upstox-java-sdk`, built into `target/upstox-fno-pipeline.jar` via the Maven Wrapper. Python scripts and `.venv` have been removed. Verified live: `pipeline` (token + full snapshot) runs clean; all 19 snapshot sections populate for NIFTY/BANKNIFTY/SENSEX.
- The Java `MarketDataStreamerV3` decodes the protobuf V3 feed for us (no manual protobuf). API behaviour preserved 1:1: historical candles use `getHistoricalCandleData1` (with `fromDate`); candle `interval` is an `int`; option-chain expiry is bare `YYYY-MM-DD` picking nearest expiry > today; Upstox news API stays out of the snapshot (news comes from the news-scanner agent). Note: V3 intraday history is capped (~1500 5m bars; ~90-day span errors with `UDAPI1148`).
- Daily token still required (browser OAuth + 2FA done by the user). Some authenticated endpoints (market status, option chain) need a fresh same-day token; the historical-candle endpoint is more lenient.
